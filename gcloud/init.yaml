---
includes:
  - drivers.yaml

{%- if empty .options.without_gcloud_gke %}
  - gke.yaml
  - contexts.yaml
  - workflows.yaml
{%- end %}
{%- if empty .options.without_gcloud_dataflow %}
  - dataflow.yaml
{%- end %}
